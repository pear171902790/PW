<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        html, body {
            width: 100%;
            height: 100%;
            font-family: 微软雅黑, 宋体, Arial, Helvetica, sans-serif !important;
        }

        a:link {
            color: @ViewBag.aColor;
            text-decoration: none;
        }

        a:active {
            color: @ViewBag.aColor;
        }

        a:visited {
            color: @ViewBag.aColor;
            text-decoration: none;
        }

        a:hover {
            color: @ViewBag.aColor;
            text-decoration: none;
        }
    </style>
</head>
<body>
    @RenderBody()
</body>
</html>
